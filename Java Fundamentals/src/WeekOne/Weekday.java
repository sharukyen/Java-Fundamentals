package WeekOne;

public enum Weekday {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
}
class weekTest{
    public static void main(String args[]) {
        Weekday a_day = Weekday.FRIDAY;
        System.out.println(a_day);
    }
}
